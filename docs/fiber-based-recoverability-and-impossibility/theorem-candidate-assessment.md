# Theorem-Candidate Assessment

## Strongest surviving identifiability statement

The best current positive universal statement is still:
- exact target identifiability is fiber constancy / factorization through the record map.

The best current negative layer is now:
- amount-only or budget-only exactness classifiers fail,
- exactness on a smaller family does not certify exactness on an enlarged one,
- and exactness of the true family does not make a mismatched inverse map safe.

## Best current theorem package

1. `OCP-049` no rank-only exact classifier theorem
2. `OCP-050` no fixed-library budget-only exact classifier theorem
3. `OCP-051` noisy weaker-versus-stronger separation theorem
4. `OCP-052` restricted-linear family-enlargement false-positive theorem
5. `OCP-053` canonical model-mismatch instability theorem

## Strongest new theorem from this pass

### `OCP-053`
Why it matters:
- it is explicitly inverse-problem shaped,
- it distinguishes target identifiability from inverse-map robustness,
- it gives an exact closed-form model-mismatch error law on a supported family.

## Still not promoted

Not promoted beyond `OPEN` or `CONDITIONAL`:
- universal model-mismatch robustness theorems,
- nonlinear identifiability extensions,
- broad PDE observability equivalence theorems,
- universal discretization-stability laws.

## Best next theorem target

Most realistic next theorem target:
- weighted-cost or geometry-constrained anti-classifier theorems for sensor design,
- or one bounded-domain family-enlargement theorem on a richer supported PDE-side family.
