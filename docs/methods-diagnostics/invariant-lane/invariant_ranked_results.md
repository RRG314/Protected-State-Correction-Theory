# Invariant Ranked Results

Status: `EXPLORATION / NON-PROMOTED`  
Date: 2026-04-18

Ranking criteria:
- theorem power
- explanatory power
- distinctness from amount-only descriptors
- robustness under falsification
- publication potential

## 1) Strongest Exact Invariant

**Winner:** Fiber/factorization exactness + row-space/kernel compatibility pair.

- Status: `PROVED`
- Why it wins: exact iff characterization, no ambiguity in supported classes.
- Limitation: foundational and largely known in substance.

## 2) Strongest Quantitative Invariant

**Winner:** CID (context-invariance defect) in context-sensitive families.

- Status: `PROVED ON SUPPORTED FAMILY` as zero-test equivalence.
- Why it matters: captures local-vs-shared decoder failure continuously.
- Limitation: novelty risk if presented beyond scoped context package.

## 3) Strongest Repair/Augmentation Invariant

**Winner:** `delta_free` (with `delta_C` complement).

- Status:
  - `delta_free`: `PROVED ON SUPPORTED FAMILY`
  - `delta_C`: `PROVED ON SUPPORTED FAMILY`
- Why it matters: constructive repair threshold plus constrained-library impossibility signal.
- Limitation: closed-form global bounds still open.

## 4) Strongest Anti-classifier Invariant

**Winner:** IDELB + DFMI + CL package.

- Status: `PROVED ON SUPPORTED FAMILY`.
- Why it matters: quantifies descriptor ambiguity and measurable lift from compatibility information.
- Key evidence:
  - rank-only ambiguity rate `1.0`, IDELB `0.5`
  - rank+compatibility proxy ambiguity `0.0`, IDELB `0.0`

## 5) Strongest Context-sensitive Invariant

**Winner:** Context gap + CID + agreement-lift consistency bundle.

- Status: `PROVED ON SUPPORTED FAMILY`.
- Why it matters: directly encodes conditioned-vs-invariant split.
- Key evidence:
  - `506` local-exact/global-fail rows in multicontext catalog.

## 6) Strongest Branch-specific Physics Invariant

**Winner:** Boundary/domain compatibility indicators (CFD/MHD branch).

- Status: `CONDITIONAL`.
- Why selected: currently most plausible physics-side invariant class tied to actual recoverability failures.
- Limitation: no unified theorem coupling yet.

## 7) Strongest Candidate to Drop (or keep archival-only)

**Winner:** Broad generic “alignment invariant” claims that do not outperform row-space/fiber criteria.

- Status: `REDUNDANT` unless tied to explicit additive quantity (`delta_C`, CL, or branch-specific diagnostics).

## Overall ranking (top-to-bottom)

1. Fiber/factorization + row-space exact invariant pair
2. `delta_free` / `delta_C` augmentation-deficiency pair
3. Context gap + CID + agreement-lift package
4. IDELB/DFMI/CL descriptor-fiber package
5. Family-enlargement/mismatch fragility indicators
6. Boundary/domain compatibility candidates (conditional)
7. Broad unspecific alignment descriptors (drop or demote)
