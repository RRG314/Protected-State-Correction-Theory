# Theorem/Operator Ranked Results

## 1. Strongest theorem-ready result

Result:
- conditioned-vs-invariant recoverability split + descriptor-only insufficiency package.

Statement:
- there exist supported families with per-context exactness and shared-decoder failure,
- and amount-only descriptor class fails to classify invariant exactness.

Why it matters:
- this is the clearest additive theorem/no-go package across current core lanes.

Status:
- `PROVED ON SUPPORTED FAMILY`, `PLAUSIBLY DISTINCT` package.

## 2. Strongest no-go theorem

Result:
- candidate-library defect impossibility + no amount-only exact classifier.

Why it matters:
- blocks both descriptor-only and library-amount-only shortcuts:
  `delta_C > 0` forbids repair even when raw rank gain looks sufficient.

Status:
- `PROVED ON RESTRICTED CLASS` (library defect law) + `PROVED ON SUPPORTED FAMILY` (descriptor anti-classifier).

## 3. Strongest supported-family theorem outside context core

Result:
- MHD variable-resistivity obstruction with restricted annular survivor classes.

Why it matters:
- strongest non-OCP-core theorem lane with explicit status separation.

Status:
- `PROVED ON SUPPORTED FAMILY`, branch-limited scope.

## 4. Strongest genuinely useful operator

Result:
- Augmentation Gain Operator (AGO): `CID_before - CID_after` under fixed admissibility class.

Why it matters:
- ranks practical augmentation impact and tracks threshold behavior.

Status:
- `PROVED ON SUPPORTED FAMILY` (positive-gain existence),
- novelty `PLAUSIBLY DISTINCT` but theorem closure incomplete.

## 5. Strongest genuinely useful projection method

Result:
- stack projection + projection-sufficiency failure diagnostic pair.

Why it matters:
- provides concrete negative result: projection alignment is insufficient for shared compatibility.

Status:
- utility `PROVED ON SUPPORTED FAMILY` for insufficiency witness,
- method itself `ALREADY KNOWN IN SUBSTANCE`.

## 6. Strongest candidate equation needing one more proof step

Result:
- constrained augmentation threshold closure (`r_*` bounds under fixed admissibility class).

Needed next step:
- derive necessary/sufficient condition for `r*=1` and tight lower/upper bounds for `r*>1` in library-constrained settings.

Status:
- `CONDITIONAL` / `OPEN` for closed-form law.

## 7. Strongest object to reject as rebranding

Result:
- SDCO/CLE/CID as “new operator theory.”

Why reject:
- they are equivalent reformulations of existing shared feasibility/fiber compatibility logic in linear class.

Status:
- `REDUCES TO EXISTING OCP LOGIC`, broad novelty claim `DISPROVED / COLLAPSED`.

## Priority ranking for immediate paper development

1. Context-sensitive split + anti-classifier no-go package.
2. Shared augmentation threshold bounds + candidate-library defect characterization.
3. Restricted MHD obstruction/survivor theorem package (with strict scope).
