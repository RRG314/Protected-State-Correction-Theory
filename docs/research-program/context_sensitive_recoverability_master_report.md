# Context-Sensitive Recoverability Master Report

Status: `EXPLORATION / NON-PROMOTED`
Date: 2026-04-18

## Executive Verdict

Final class: **KEEP AS CONDITIONAL THEORY PACKAGE**.

Interpretation:
- The main three-package narrow theory survives as a coherent conditional program on supported families.
- It is not yet promotion-ready as a canonical spine layer due synthetic dependence and incomplete augmentation proofs.
- Formation work remains meaningful but secondary and non-canonical.

## Exact Scope

This package is scoped to:
- finite/synthetic linear and context-structured families,
- target-specific recoverability under context-indexed observation maps,
- conditioned-vs-invariant decoder distinctions,
- shared augmentation and multi-context anomaly analysis under fixed budgets.

## Main Track Status

## 1) Conditioned-vs-Invariant Theorem Package

Current status:
- strict split existence: `PROVED ON SUPPORTED FAMILY`
- shared-decoder feasibility criterion: `PROVED` (characterization)
- descriptor-only no-go for split classification: `PROVED ON SUPPORTED FAMILY`

Evidence:
- `506` local-exact/global-fail families,
- `23` same-descriptor opposite shared-verdict groups.

## 2) Shared Augmentation Threshold Package

Current status:
- positive threshold existence: `PROVED ON SUPPORTED FAMILY`
- threshold distribution in current class: `r*=1` (`347`), `r*=2` (`159`)
- exact closed-form law: `CONDITIONAL / OPEN`
- constrained-library defect law (`delta_C`) for full-pool feasibility: `PROVED ON RESTRICTED CLASS`
- library gain insufficiency counterexample: `PROVED ON RESTRICTED CLASS`

Key caveat:
- threshold depends on admissible augmentation class.
- `library_rank_gain` can match/exceed free-threshold rank need and still fail due direction mismatch (`delta_C > 0`).

## 3) Multi-Context Failure / Opposite-Verdict Package

Current status:
- same-descriptor opposite-verdict catalog: `PROVED ON SUPPORTED FAMILY`
- family enlargement fragility: `VALIDATED / EMPIRICAL ONLY`

Evidence:
- `94` enlargement flips (`1 -> 0` shared exactness).

## What Survived Falsification

1. conditioned-vs-invariant split is robust in supported families,
2. amount-only descriptors are insufficient,
3. augmentation need is real and positive for local-exact/global-fail families,
4. multi-context failure families are structurally reproducible.

## What Collapsed into Existing OCP Logic

1. core exactness/collapse mechanics remain row-space/fiber based,
2. CID as a zero-test is largely a reformulation of shared-decoder feasibility,
3. broad novelty claims beyond OCP backbone do not survive.

## What Is Genuinely Additive

1. disciplined packaging of contextwise-vs-invariant recoverability split,
2. explicit shared augmentation threshold catalogs with scope labels,
3. constrained augmentation-library no-go certificates (`delta_C > 0`),
4. reproducible multi-context opposite-verdict anomaly families.

## Secondary Formation Track Outcome

Status: `CONDITIONAL / EXPLORATORY / NON-CANONICAL`.

Meaningful outputs:
- formation mechanisms can create local-not-shared regimes (`280` cases),
- formation can induce augmentation-threshold flips (`632` cases).

Limitations:
- no independent bridge theorem ready,
- several mechanisms remain template-sensitive.

## Bridge-Theorem Readiness

Current answer: **not ready**.

Bridge evidence is empirical and useful, but it does not yet produce a clean theorem independent of main-track context-compatibility objects.

## Promotion Guidance

Promote later only if all of the following are added:
1. non-synthetic benchmark confirmation,
2. proof-level lower/upper bounds for shared augmentation threshold,
3. stronger robustness against family enlargement and perturbation protocols.

Until then:
- keep main track conditional but central,
- keep formation track separate and exploratory.

## Codex Recommendation — What should happen next?

Chosen next move: **(2) deepen augmentation theorem before promotion**.

Reason:
- the context-split and opposite-verdict packages are already strong on supported families,
- the promotion bottleneck is the augmentation law (closed-form bounds and admissible-class dependence),
- formation should continue separately but should not gate narrow-theory formalization.

Secondary policy (non-competing): keep formation entirely separate for now unless one bridge theorem reaches proof-level support.

## Continuation Addendum (2026-04-18, deep pass)

Artifacts:
- `data/generated/context_sensitive_recoverability/agreement_operator_witness_catalog.csv` (`300` rows)
- `data/generated/context_sensitive_recoverability/agreement_operator_anomaly_catalog.csv` (`87` rows)

Continuation highlights:
1. agreement-lift/direct solver consistency remained exact (`300/300`),
2. descriptor opposite-verdict persistence strengthened (`59` groups),
3. candidate-library defect no-go produced explicit impossibility cases (`14`),
4. library-rank-gain insufficiency matched the same `14` cases (amount-only library metrics fail).
