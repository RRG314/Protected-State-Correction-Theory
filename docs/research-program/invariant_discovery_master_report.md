# Invariant Discovery Master Report

Status: `EXPLORATION / NON-PROMOTED`  
Date: 2026-04-18

## Executive Verdict

Final verdict class: **CONDITIONAL INVARIANT PROGRAM + TOOL LAYER**.

Interpretation:
- No universal new core invariant displaced the existing fiber/row-space backbone.
- The strongest progress is a scoped quantitative invariant program around context coherence, augmentation deficiency, and descriptor-lift ambiguity reduction.

## 1) What invariants already existed?

1. Fiber constancy/factorization exactness.
2. Row-space/kernel compatibility and null-intersection criteria.
3. Minimal augmentation threshold concepts.
4. Collision-gap/collapse diagnostics.
5. Descriptor-fiber anti-classifier metrics (DFMI/IDELB/CL).
6. Context-sensitive CID/context-gap diagnostics.

## 2) Which existing invariants needed clarification or expansion?

1. Collision-gap needed computational regime labeling (`exact_low_null_dim` vs proxy mode).
2. Augmentation deficiency needed explicit split between free threshold and candidate-library defect.
3. Descriptor-lift needed clearer scope (catalog-level theorem, not universal guarantee).
4. Context gap needed explicit relation to shared decoder feasibility and CID.

## 3) Which new invariants were found?

Most credible new/extended candidates:
1. `delta_C` (candidate-library defect) as constrained no-go invariant.
2. Context-coherence residual formulation (CCD, operationalized as CID).
3. Family-enlargement fragility index (candidate quantitative diagnostic).
4. Mismatch instability slope (candidate, not mature).

## 4) Which are theorem-grade now?

Theorem-grade on supported classes:
- CID zero-test for shared exactness.
- Positive shared augmentation thresholds (`delta_free`) in local-exact/global-fail families.
- Candidate-library defect no-go (`delta_C > 0` implies infeasibility in tested library classes).
- Descriptor anti-classifier lower bounds and compatibility lift improvements on generated catalogs.

## 5) Which are only diagnostic?

1. High-null collision-gap proxy outputs.
2. Family-enlargement fragility rate laws.
3. Mismatch instability slope.
4. Mixedness-depth refinements beyond DFMI/IDELB.

## 6) Which collapsed?

1. Any broad “alignment invariant” claim that does not add beyond row-space/fiber logic.
2. Any amount-only descriptor claim of exact recoverability classification.

## 7) What is currently strongest overall?

Strongest overall invariant stack:
- Fiber/factorization exactness + row-space/kernel compatibility (exact core),
- augmented by context-sensitive CID/context-gap + `delta_free`/`delta_C` for constructive and multi-context structure.

## 8) Strongest beyond rank/count/budget?

The strongest beyond amount-only descriptors is the **descriptor+compatibility lift program**:
- IDELB reduction by CL,
- context coherence diagnostics (CID),
- constrained repair deficiency (`delta_C`).

## 9) Most promising invariant for positive theorem development

Highest-priority lane:
- **Augmentation-threshold invariant program (`delta_free`, `delta_C`)** with context-coherence coupling.

Reason:
- strongest path from no-go to constructive recovery design,
- best evidence of additive decision power beyond raw amount descriptors.

## 10) Exact invariant program to push next

Push a narrow invariant program with four layers:
1. exact core: fiber/factorization + row-space,
2. context coherence: CID + context-gap split,
3. repair law: `delta_free` + `delta_C`,
4. anti-classifier quantification: DFMI/IDELB/CL.

Keep stress and branch-side invariants as explicit diagnostic layers.

## Key evidence highlights

- Multicontext catalog (`1800` rows):
  - local-exact/global-fail rows: `506`
  - shared exact rows: `113`
- Augmentation catalog (`506` rows):
  - threshold `1`: `347`
  - threshold `2`: `159`
- Descriptor meta:
  - rank-only ambiguity rate `1.0`, IDELB `0.5`
  - rank+compatibility proxy ambiguity `0.0`, IDELB `0.0`
- Agreement-operator anomalies:
  - same-descriptor opposite verdict: `59`
  - candidate-library defect impossibility: `14`
  - library-gain-not-sufficient: `14`
- Invariant stress pass (`18` rows):
  - fragility flips: `18/18` across mismatch/enlargement/noise stressors

## Safe claims

1. “Amount-only descriptors do not classify recoverability on supported families.”
2. “Context coherence and augmentation deficiency provide additive decision power in supported context-indexed linear classes.”
3. “Candidate-library geometry can block repair even when unconstrained augmentation succeeds.”

## Unsafe claims

1. “A universal new recoverability invariant has been found.”
2. “Alignment quantities supersede row-space/fiber exactness.”
3. “All stress fragility rates generalize beyond tested families.”

## Best serious next move for the invariant program

Recommended move: **(5) push augmentation-threshold invariant next**.

Concrete next steps:
1. Prove tighter lower/upper bounds for `delta_free` in context-indexed linear classes.
2. Formalize `delta_C` necessary/sufficient conditions for candidate-library feasibility.
3. Integrate CID + (`delta_free`,`delta_C`) into one theorem-backed design checklist.
4. Expand stress tests to non-synthetic benchmark families before promotion.
