# New Math Literature Audit

Status: literature-risk audit for theorem/operator/projection/equation outputs from this pass.

## 1. Conditioned-vs-Invariant Recoverability Package

Nearest literature zones:
- observability/identifiability across regimes,
- multi-model estimation and calibration mismatch,
- data-driven control compatibility constraints.

Overlap assessment:
- mechanism is close to known compatibility/consistency themes.
- current contribution is strict theorem/no-go packaging with explicit shared-decoder equations and witness catalogs.

Classification:
- `PLAUSIBLY DISTINCT` as scoped package,
- `CLOSE PRIOR ART / REPACKAGED` at mechanism level.

## 2. Descriptor-Only Insufficiency / Anti-Classifier Corpus

Nearest literature zones:
- impossibility of single-summary classifiers,
- identifiability failures with matched rank/count statistics.

Overlap assessment:
- concept is known in substance in multiple fields.
- contribution is reproducible finite witness corpora with branch-disciplined status labeling.

Classification:
- `PLAUSIBLY DISTINCT` finite-package contribution,
- `CLOSE PRIOR ART / REPACKAGED` conceptually.

## 3. Shared Augmentation Threshold

Nearest literature zones:
- sensor placement and experiment design,
- minimal augmentation/observability completion,
- structured matrix completion of identifiability conditions.

Overlap assessment:
- “add measurements until recoverability” is known.
- distinct part is explicit shared-across-context threshold formulation with declared admissibility class and anomaly catalogs.

Classification:
- `PLAUSIBLY DISTINCT` on supported family,
- theorem strengthening still `OPEN` (closed-form bounds).

## 4. Projection Candidates

Nearest literature zones:
- projection methods, reduced-order modeling, PDE projection.

Overlap assessment:
- stack row-space and residual projections are standard.
- no new projection theorem/object survived as distinct.

Classification:
- `ALREADY KNOWN IN SUBSTANCE` / `REDUCES TO EXISTING OCP LOGIC`.

## 5. Operator/Equation Candidates (SDCO, CID, CLE)

Nearest literature zones:
- linear operator equations,
- least-squares consistency, compatibility defects.

Overlap assessment:
- all core definitions reduce to known linear-algebraic feasibility objects in the declared class.

Classification:
- `REDUCES TO EXISTING OCP LOGIC`.

## 6. CFD Geometry-Sensitive Split

Nearest literature zones:
- inverse problems with sensor geometry dependence,
- bounded-domain observability/projection limits.

Overlap assessment:
- known direction in substance,
- current branch adds explicit descriptor-matched opposite-verdict witnesses in this repo’s exactness language.

Classification:
- `PLAUSIBLY DISTINCT` package within branch scope.

## 7. MHD Variable-Resistivity Obstruction (Restricted Classes)

Nearest literature zones:
- MHD closure with resistive terms,
- Euler-potential closure obstructions.

Overlap assessment:
- area has prior art pressure, but current restricted theorem set appears substantive in declared classes.

Classification:
- `PLAUSIBLY DISTINCT` (restricted-class theorem package),
- avoid global extrapolation.

## Safe wording summary

Safe:
- “proved on supported families,”
- “branch-limited theorem/no-go package,”
- “computationally useful diagnostics that reduce to existing compatibility logic.”

Unsafe:
- “new operator theory,”
- “universal projection law,”
- “global closure/classification theorem across unrestricted domains.”
