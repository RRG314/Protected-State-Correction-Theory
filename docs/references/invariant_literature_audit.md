# Invariant Literature Audit

Status: `EXPLORATION / NON-PROMOTED`  
Date: 2026-04-18

This is a concept-level overlap audit for surviving invariants. It is a risk-positioning document, not a novelty claim.

## 1. Fiber constancy / factorization exactness

Nearest literature zones:
- identifiability in inverse problems
- sufficient-statistic/factorization viewpoints
- deterministic observability/reconstructibility in linear systems

Assessment:
- `KNOWN IN SUBSTANCE`.
- Repo value is disciplined cross-branch packaging and explicit no-go constructions.

## 2. Row-space/kernel compatibility residuals

Nearest literature zones:
- linear algebraic observability
- reconstruction from linear functionals
- projection residual criteria

Assessment:
- `KNOWN IN SUBSTANCE`.
- Repo contribution is not novelty of criterion, but sustained anti-classifier and fragility program built around it.

## 3. Minimal augmentation deficiency (`delta_free`)

Nearest literature zones:
- sensor placement / observability augmentation
- experimental design under structural constraints

Assessment:
- likely `CLOSE PRIOR ART / REPACKAGED` at broad concept level.
- current scoped contribution remains plausible in explicit multi-context recoverability framing with exact local-vs-shared split.
- label as `PLAUSIBLY DISTINCT RESTRICTED INVARIANT` pending formal comparisons.

## 4. Candidate-library defect (`delta_C`)

Nearest literature zones:
- constrained design dictionaries
- feature/library completeness in inverse design and control sensing

Assessment:
- structure is close to rank-defect ideas, but current exact no-go framing in shared-decoder context appears more specific.
- classify as `PLAUSIBLY DISTINCT RESTRICTED INVARIANT`.

## 5. Descriptor-fiber invariants (DFMI/IDELB/CL)

Nearest literature zones:
- classification ambiguity bounds
- Bayes/minimax lower-bound style arguments for insufficient summaries

Assessment:
- ambiguity lower-bound concept is `KNOWN IN SUBSTANCE`.
- descriptor-fiber packaging tied to recoverability witness catalogs is `PLAUSIBLY DISTINCT PACKAGING`.

## 6. Context-coherence/CID invariants

Nearest literature zones:
- multi-model/shared-parameter consistency
- compatibility of local estimators under shared decoder constraints

Assessment:
- CID as residual minimization resembles known consistency defects.
- specific local-exact/global-fail theorem packaging in this repo is `PLAUSIBLY DISTINCT RESTRICTED RESULT`.

## 7. Enlargement/mismatch fragility indicators

Nearest literature zones:
- model misspecification robustness
- domain shift and distribution shift in inference/control

Assessment:
- `KNOWN IN SUBSTANCE` at high level.
- branch contribution is explicit constructive witness families and quantification hooks.

## 8. Boundary/domain compatibility (CFD/MHD)

Nearest literature zones:
- projection method compatibility
- boundary condition sensitivity and closure obstructions

Assessment:
- likely `CLOSE PRIOR ART / REPACKAGED` unless a sharper invariant theorem is formalized.
- keep branch-limited for now.

## 9. Quantum alignment ratios

Nearest literature zones:
- quantum Fisher/classical Fisher geometry
- target-specific measurement design

Assessment:
- mostly `KNOWN / REFRAMED`.
- narrow restricted identities may be `POSSIBLY IMPLICIT KNOWN SPECIAL CASE`.

## Safe wording for public-facing claims

Use:
- “within supported finite linear/context-indexed families”
- “anti-classifier evidence and augmentation thresholds are reproducible in generated witness catalogs”
- “descriptor-lift reduces irreducible ambiguity on current catalogs”

Avoid:
- “new universal invariant of recoverability”
- “first general solution to recoverability classification”
- “novel beyond prior observability/identifiability literature” without theorem-level comparative proof.
