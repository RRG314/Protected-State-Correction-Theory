# High-Priority Next Problems

## 1. Robust Restricted PVRT
This is now the single strongest next theorem-completion problem because the constrained-observation branch finally supports a real restricted theory spine rather than only family-level examples.

Current upgrade:
- exactness on fibers is already formalized
- `κ(η)/2` lower bounds are already proved
- the exact-regime upper envelope now exists in the exact restricted-linear branch
- same-rank insufficiency now cleanly falsifies record-amount language
- minimal augmentation is already solved on restricted linear families

Remaining open target:
- extend the restricted-linear PVRT spine to noisy records or admissible-family enlargement without collapsing into weak generalities

Why it matters:
- this is the nearest route to turning the strongest observation-layer branch into a broader theorem program
- it is also the cleanest place where the repo may still produce a real new contribution without bluffing

## 2. Boundary-Sensitive Exact Continuous Correction
This remains a top structural problem because it is the nearest route to materially extending the exact continuous branch without changing the theory's character.

Current upgrade:
- a restricted positive result now exists on the repository's boundary-compatible finite-mode Hodge family
- the remaining open target is the broader bounded-domain theorem tied to realistic domain-compatible projectors and boundary data

CFD consequence:
- the right next question is not “all of CFD.” It is whether a domain-compatible Hodge projector theorem can be made explicit for bounded incompressible classes with real boundary data.

## 3. Mature Branch-Specific Capacity Theory
This is the main theorem-completion problem. The repository now has real lower bounds and structural summaries, and the restricted-linear design layer now has a proved minimal-augmentation invariant. The remaining task is to mature the rest of the category-specific capacity layer without reviving the failed universal scalar program.

## 4. One Stronger Sector Anchor If It Sharpens The Formalism
A second exact sector anchor is only worthwhile if it improves the theorem language or the operator construction, not if it merely duplicates the bit-flip story.

## 5. Conditional Control Branch Tightening
A careful invariant-split observer/controller theorem could add engineering value, but only if it keeps the protected/disturbance split explicit and avoids generic feedback language.
