# Canonical Witnesses and Counterexamples

Date: 2026-04-17

This file records one canonical witness/counterexample for each sharpened branch-limited result.

## W-1 (No rank-only classifier)

Witness type:
- two restricted-linear systems with equal rank tuple, opposite exactness verdict under `ker(OF) ⊆ ker(LF)` test.

Use:
- canonical no-rank-only classifier witness set from `OCP-049` package.

Status:
- `PROVED` (as theorem-backed witness class).

## W-2 (No fixed-library budget-only classifier)

Witness type:
- same sensor budget inside one fixed candidate library, opposite exactness outcomes.

Use:
- canonical fixed-library budget witness set from `OCP-050` package.

Status:
- `PROVED`.

## W-3 (Family-enlargement false-positive)

Witness type:
- exact on `A_s`, fails on enlarged `A_l` due to new collision direction.

Use:
- `OCP-052` theorem witness construction and enlarged-family lower bound.

Status:
- `PROVED`.

## W-4 (Model mismatch instability)

Witness type:
- decoder exact for family parameter `beta0` evaluated on true family `beta ≠ beta0` with explicit error floor.

Use:
- canonical mismatch formula from `OCP-053`.

Status:
- `PROVED` (declared canonical family).

## W-5 (Bounded-domain architecture mismatch)

Witness type:
- periodic-style projector transplant reduces divergence but violates bounded protected-class boundary compatibility.

Use:
- `OCP-023` and `OCP-028` no-go pair.

Status:
- `PROVED`.

## W-6 (Restricted bounded-domain exact positive witness)

Witness type:
- boundary-compatible finite-mode Hodge family where exact correction is recovered.

Use:
- `OCP-044` restricted exactness witness.

Status:
- `PROVED ON SUPPORTED FAMILY`.

## W-7 (Soliton same-count opposite-verdict)

Witness type:
- equal observation count families with opposite identifiability/recoverability verdicts on restricted one-soliton manifold scans.

Use:
- supported finite-family witness package from soliton status registry.

Status:
- `PROVED ON SUPPORTED FAMILY` + `CONDITIONAL` continuous extension.

## W-8 (Soliton projection split)

Witness type:
- low-pass/operator class near-preserving in one scenario, explicit no-go under alternate operator/scenario pair.

Use:
- projection lane summary artifacts.

Status:
- `VALIDATED` + `CONDITIONAL` theorem.

## W-9 (MHD annular-vs-axis obstruction witness)

Witness type:
- variable-`η(r)` radial classes with nontrivial annular survivors and no smooth nonconstant axis-touching survivors in supported classes.

Use:
- MHD Theorems 4.1/4.2 and corollary package.

Status:
- `PROVED ON SUPPORTED FAMILY` (with declared-class proved corollaries).
