# Meta-Theory Promotion Decision

Date: 2026-04-17
Pass: Meta-theory extraction

## Decision summary

The current meta-theory is **mostly interpretive at the slogan level**, but it can be promoted through one real mathematical extraction:

1. a descriptor-fiber anti-classifier theorem package,
2. a quantitative invariant layer (`DFMI`, `IDELB`, `CL`),
3. a minimal failure-mode taxonomy with collision-reducible vs non-collision classes.

## Required questions

### 1) Is the current meta-theory mostly rewording, or can it produce new mathematics?

- Mostly rewording at broad narrative level: **yes**.
- Produces a real new mathematical layer after extraction: **also yes**.

So the correct conclusion is mixed:
- broad meta language -> `INTERPRETIVE ONLY`,
- extracted descriptor-fiber package -> `PROMOTABLE`.

### 2) If yes, what is the single strongest real addition?

**Single strongest addition:**

Finite-class descriptor-fiber anti-classifier theorem + irreducible amount-only error bound:

- perfect descriptor-only classification exists iff descriptor fibers are verdict-pure,
- minimum descriptor-only error equals the minority-mass sum across descriptor fibers.

This yields computable invariants (`DFMI`, `IDELB`) and a compatibility-gain metric (`CL`).

### 3) Which form should be promoted?

Primary promotion form:
- **classification + quantitative invariant package**.

Secondary support form:
- **counterexample package** (same-descriptor opposite-verdict witnesses) already available and now organized by descriptor-fiber metrics.

Not promoted as main form:
- a new universal theorem layer across all branches.

### 4) If no strong addition survived, should this remain interpretation-only?

Not applicable as written: a strong branch-limited addition did survive.

But explicit scope guard:
- keep meta-theory slogans themselves as `INTERPRETIVE ONLY`,
- promote only the extracted theorem/invariant package.

## Status labels

- Descriptor-fiber purity theorem (`MAC-1`): `PROVED`.
- Irreducible descriptor error bound (`MAC-2`): `PROVED ON SUPPORTED FAMILY` (finite witness classes).
- Quantitative invariants `DFMI`, `IDELB`, `CL`: `PROVED USEFUL` on supported datasets.
- Universal anti-classifier theorem across all branches: `REJECTED`.
- Strong universal multi-role thesis: `REJECTED`.

## Promotion action

Promote as new branch-limited addition and document in:
- `papers/meta_theory_real_addition.md`.
