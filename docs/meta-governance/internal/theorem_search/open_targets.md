# Open Targets (OCP)

1. Explicit bound linking model-mismatch subspace distance to worst-case decoder error (`CONDITIONAL` -> target `PROVED ON SUPPORTED FAMILY`).
2. Tighter quantitative obstruction law coupling principal-angle defect and collision-gap lower bounds.
3. Bridge target: formal criterion for when nonlinear same-count opposite-verdict behaves as a true OCP restricted analogue.
