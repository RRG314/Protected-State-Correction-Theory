# SFPR Internal Coherence Report

Status: `EXPLORATION / NON-PROMOTED`

## Coherence Questions (forced yes/no)

### 1) Can one consistent notation handle all six layers?
Answer: **YES (conditionally).**

Reason:
- The object tuple `(X, tau, M, C, R, D)` can encode collapse/recoverability/design and much of persistence.
- Formation still needs typed mechanism classes; without those, notation becomes rhetorical.

### 2) Do formation, persistence, collapse, recoverability, and design interact through shared objects?
Answer: **YES (partially).**

Reason:
- Shared objects: `tau`, `M`, context family `C`, and recovery constraints.
- Empirical support: `4560` witnesses, `1669` anomalies, with linked split mechanisms (`conditioned_vs_invariant_split_support = 224`, `design_failure_support = 38`, `intervention_split_support = 210`).
- Weakness: formation layer currently interacts mostly through empirical deltas, not theorem-grade bridges.

### 3) Are some layers independent enough that they should be separate theories instead?
Answer: **YES.**

Reason:
- Formation currently behaves like an adjacent dynamical program.
- Collapse/recoverability/design are tightly coupled and theorem-ready at branch-limited scope.

### 4) Is OCP naturally one sublayer inside the larger theory, or does the larger theory collapse back to OCP?
Answer: **BOTH: OCP is a natural core, and broad SFPR collapses toward OCP when overgeneralized.**

Operationally:
- Narrow SFPR survives as an extension around OCP core objects.
- Broad claims reduce to existing OCP/fiber logic in supported linear classes.

### 5) Does TSIT naturally belong inside the design/recoverability layer, or is it too separate?
Answer: **YES, it belongs inside design/recoverability as an adjacent lane.**

Reason:
- TSIT conflicts and allocation/intervention diagnostics map directly to layer VI (design) and layer V (recoverability).
- Not independent enough to replace core theory; correct placement remains Option D-style adjacent quantitative extension.

### 6) Does context-invariant recoverability fit as a persistence or recoverability theorem family?
Answer: **YES, primarily persistence-to-recoverability bridge.**

Reason:
- It captures local exact / global fail behavior under context changes (layer III -> V).

### 7) Are “structure” and “target-relevant structure” different enough that the theory must choose one?
Answer: **YES.**

Decision:
- SFPR must use **target-relevant structure** as primary object.
- Generic structure language is too broad and causes collapse into narrative.

## Coherence Verdict

- **Not coherent as a broad universal theory.**
- **Coherent as a narrow layered candidate** centered on target-relative collapse/recoverability/design, with persistence bridges and a still-weak formation layer.

## Practical Split Recommendation

1. Keep a narrow SFPR core: collapse + recoverability + design + context-invariant persistence.
2. Keep formation as separate conditional subprogram until theorem-grade bridges are proved.
