# Master Theorem Search Report (OCP)

Date: 2026-04-17

## Promoted OCP theorem targets this pass
1. Rank-only exact-classifier no-go package (`PROVED ON SUPPORTED FAMILY`).
2. Family-enlargement false-positive theorem package (`PROVED ON SUPPORTED FAMILY`).

## Strengthened but not fully promoted
1. Canonical model-mismatch instability envelope (`CONDITIONAL`).
2. Rank-fragility quantitative split (`VALIDATED`).

## Nonlinear bridge theorem candidates (tracked only)
1. Soliton same-count opposite-verdict (`PROVED ON SUPPORTED FAMILY`, bridge-relevant).
2. Projection preservation/no-go split (`VALIDATED`, bridge-relevant).

These bridge candidates remain outside OCP core theorem promotion unless internal OCP formalism captures them without analogy-only steps.
